{
  "name": "ts-learning",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "run": "npx ts-node TestTS001.ts",
    "run:tsnode": "npx ts-node --transpile-only TestTS001.ts",
    "build:run": "npx tsc TestTS001.ts --outDir dist && node dist/TestTS001.js"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "ts-node": "^10.9.2",
    "typescript": "^7.0.2"
  }
}
